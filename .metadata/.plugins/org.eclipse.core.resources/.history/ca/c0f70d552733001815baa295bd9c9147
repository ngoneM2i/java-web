package hello;

public class UtilNote {
	
	public double calcMoyenne(int[] notes){
		int sum = 0;
				
		for (int i = 0; i < notes.length; i++) {
			sum += notes[i];
		}
			double moyenne = 1.0 * sum / notes.length; //oubien on fait (double) somme / notes.length;
		
		}
	
}
